
git
git push --force origin main

DNS
日常上网（混合国内外）	223.5.5.5（阿里） + 119.29.29.29（腾讯）
开发/测试/访问 GitHub 等	8.8.8.8 + 1.1.1.1


Claude ai全局设置

无效设置

- 会话压缩：需要时提示使用 /clear 命令手动清除
- 文件搜索：超过3次未找到，给出提示不在继续查找，我告诉你


会话管理
- 文档生成：文档只在必要时生成，内容精简
- 依赖提示：仅在确实需要时才提示
- 向后兼容：不需要考虑

代码规范
- 代码保持简洁直接，避免过度工程

API 响应处理规则
- 直接使用 response 作为解包后的数据，无需检查 code 或访问 data 字段
- 成功请求不展示 message 提示

示例：
final response = await userApi.updateProfile(username: username);
final user = User.fromJson(response);


cursor
# 关闭 Cursor 后执行 删除会话历史
rm -rf ~/Library/Application\ Support/Cursor/User/globalStorage/*/state.vscdb
rm -rf ~/Library/Application\ Support/Cursor/User/workspaceStorage/*/state.vscdb




